==============
DaemonSet
==============

一个node一个副本

**在ds.spec.template.spec下有个hostNetwork参数，将pod将直接使用主机的网络名称空间。这样可以直接通过node节点的地址来访问节点上的DaemonsSet了。**

-------------------
1、用途
-------------------

* 确保集群中每一个node节点，只运行一个pod副本。
* 随着集群规模的变动，而变动，比如添加进一个node，则自动再node上起一个副本提供服务。
